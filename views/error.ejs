<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('./partials/head') %>
    <title>GoRent | Error</title>
    <style>
        .error-page {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            background-color: #f8f9fa;
            padding: 1rem;
        }

        .logo {
            height: 48px;
            margin-bottom: 2rem;
        }

        .error-content {
            text-align: center;
            max-width: 400px;
        }

        .error-image {
            width: 250px;
            height: 250px;
            margin-bottom: 1.5rem;
        }

        .location-info {
            margin-top: 2rem;
            display: flex;
            align-items: center;
            color: #6c757d;
        }

        .location-icon {
            margin-right: 0.5rem;
        }

        .back-button {
            display: inline-flex;
            align-items: center;
            padding: 0.75rem 1.5rem;
            background-color: #0d6efd;
            color: white;
            border-radius: 0.5rem;
            text-decoration: none;
            transition: background-color 0.2s;
        }

        .back-button:hover {
            background-color: #0b5ed7;
            color: white;
        }

        .home-icon {
            margin-right: 0.5rem;
        }
    </style>
</head>
<body>
    <div class="error-page">
        <!-- Logo -->
        <img src="/assets/images/GoRent.png" alt="GoRent Logo" class="logo">

        <!-- Error Content -->
        <div class="error-content">
            <h1 class="fw-bold mb-2"><%= errorTitle || 'Halaman Tidak Ditemukan' %></h1>
            
            <p class="text-secondary mb-4">
                <%= errorMessage || 'Maaf, halaman yang Anda cari tidak dapat ditemukan. Silakan kembali ke halaman utama.' %>
            </p>

            <!-- Back to Home Button -->
            <a href="/" class="back-button">
                <svg class="home-icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"></path>
                    <polyline points="9 22 9 12 15 12 15 22"></polyline>
                </svg>
                Kembali ke Beranda
            </a>
        </div>
    </div>

    <%- include('./partials/script.ejs') %>
</body>
</html>